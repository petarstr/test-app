<%= form_for(comment_file) do |f| %>
  <% if comment_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_file.errors.count, "error") %> prohibited this comment_file from being saved:</h2>

      <ul>
      <% comment_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %>
    <%= f.number_field :comment_id %>
  </div>

  <div class="field">
    <%= f.label :file %>
    <%= f.text_field :file %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
